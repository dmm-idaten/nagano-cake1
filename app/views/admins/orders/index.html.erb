<%= render "layouts/admin_header" %>

<div class="container">
	<div class="row">
    <div class="col mt-5">
      <h2>注文履歴一覧</h2>
    </div>
  </div>
  <div class="row">
  	<div class="col mt-8">
  		<table class="table">
	      <thead>
		      <tr>
			      <th>購入日時</th>
			      <th>購入者</th>
			      <th>注文個数</th>
			      <th>注文ステータス</th>
		      </tr>
	      </thead>
	      <tbody>
	        <% @orders.each do |order| %>
	      	  <tr>
			        <td><%= order.created_at %></td>
			        <td>
			        	<%= order.shipping_postcode %><br>
			        	<%= order.shipping_address %><br>
			        	<%= order.shipping_name %>
			        </td>
			        <td>
			        	<%= order.order_details.amount %>
			        </td>
			        <td><%= order.order_status %></td>
			        <td><%= link_to '表示する', order_path(order.customer_id) %></td>
		        </tr>
	      	<% end %>
	      </tbody>
      </table>
  	</div>
  </div>
</div>
<%= render "layouts/footer" %>