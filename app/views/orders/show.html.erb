<%= render 'layouts/customer_header' %>

<div class="container">
	<div class="row">
    <div class="col mt-5">
      <h2>注文履歴詳細</h2>
    </div>
  </div>

  <div class="row">
  	<div class="col mt-8">
  		<label>注文情報</label>
  		<table class="table">
  			<% @order_details.each do |order| %>
		    <tr>
			    <td>注文日</td>
			    <td><%= order.created_at %></td>
			  </tr>
			  <% end %>
			</table>
		</div>
	</div>

</div>